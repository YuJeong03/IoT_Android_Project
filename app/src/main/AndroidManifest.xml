<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.iot_android_project">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_main_foreground"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_main_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".sub1"></activity>
        <activity
            android:name=".write"
            android:label="Good Care"
            android:parentActivityName=".MainActivity"
            android:theme="@style/Theme" />
        <activity android:name=".spinner_item" />
        <activity android:name=".dialo2" />
        <activity android:name=".Chatting" />
        <activity android:name=".sub" />
        <activity android:name=".join" />
        <activity android:name=".login" />
        <activity android:name=".Dialog_a" />
        <activity android:name=".Dialog_java" />
        <activity
            android:name=".fragment1_1"
            android:label="Good Care"
            android:parentActivityName=".MainActivity"
            android:theme="@style/Theme" />
        <activity
            android:name=".splash"
            android:screenOrientation="portrait"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
    </application>

</manifest>